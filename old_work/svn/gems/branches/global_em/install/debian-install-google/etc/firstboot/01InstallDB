if [ `psql -q -Upostgres ems -t -c "select count(*) from users"`  ]
    then
	echo "upgrade"
    else
        su postgres -c /usr/local/google/home/enlighted/installdb.sh > /usr/local/google/home/enlighted/installdb.log
fi
